name: CustomPlayerNametags
version: '1.2.0'
main: afx.customplayernametags.CustomPlayerNametags
api-version: '1.20'
author: AFXPlugins
description: >
  Fully customizable player nametags.

depend: [packetevents]
softdepend: [PlaceholderAPI, LuckPerms, Multiverse-Core, Multiverse-Portals]

commands:
  nametags:
    description: CustomPlayerNametags admin/management commands
    usage: /<command> <reload|update|format|editor|config>
    aliases: [customplayernametags]
  nametag:
    description: Open the nametag menu to view, toggle, or edit your own nametag
    usage: /<command>

permissions:
  customplayernametags.admin:
    description: Allows use of all CustomPlayerNametags admin commands (/nametags).
    default: op
  customplayernametags.updatenotify:
    description: Receive a message on join when a new plugin update is available.
    default: op
  customplayernametags.multiversenotify:
    description: >-
      Receive a message on join, with a clickable fix, when Multiverse-Portals
      is installed, Multiverse-Core's teleport.passenger-mode config value is
      still set to "default" (which can let a nametag's passenger entity
      prevent Multiverse-Portals from teleporting players).
    default: op
  customplayernametags.nametag:
    description: >-
      Allows use of the /nametag command (view/toggle/editor) to manage your
      own individual nametag.
    default: true
  customplayernametags.bypasslinecharacterlimit:
    description: >-
      Bypasses nametag-line-max-characters for the player. Not granted
      by default, and a wildcard (* or customplayernametags.*) does NOT
      grant it: it only takes effect when this exact node is granted to
      the player or one of their groups.
    default: false
  customplayernametags.notify.override.notify:
    description: >-
      When this player changes someone else's individual nametag format
      (via /nametags format player set|disable or the admin editor GUI), the
      affected player is always told, regardless of
      nametag-format-notify-mode (or the announce/silent argument in BOTH
      mode). Checked on the player making the change, not the one whose
      format changes. Not granted by default. Must be granted explicitly; a
      wildcard (* or customplayernametags.*) does not grant it.
    default: false
  customplayernametags.notify.override.silent:
    description: >-
      When this player changes someone else's individual nametag format
      (via /nametags format player set|disable or the admin editor GUI), the
      affected player is never told, regardless of
      nametag-format-notify-mode (or the announce/silent argument in BOTH
      mode). Checked on the player making the change, not the one whose
      format changes. Takes priority over
      customplayernametags.notify.override.notify if a player somehow has
      both. Not granted by default. Must be granted explicitly; a wildcard
      (* or customplayernametags.*) does not grant it.
    default: false